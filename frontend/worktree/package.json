{
  "name": "photobooth-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for Photobooth — ARIA, keyboard nav, responsiveness, Style Preset regression",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
